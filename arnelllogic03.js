

//  console.log # tags to form a half pyrid
// need to add each previous hash to the new increment
// skip the first element

let hastag = "#";
let hash = "#"
for (let i = 0; i < 6; i++){
console.log(hastag += hash )
}